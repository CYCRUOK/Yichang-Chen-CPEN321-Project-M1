# Server configuration
PORT=3000
NODE_ENV=development

# Database
MONGODB_URI=mongodb://localhost:27017/cpen321

# Google Auth 
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com

# JWT 
JWT_SECRET=your_jwt_secret_key_here

# Other API keys, secrets, and configurations
# M1 Button 1 APIs
# Your name, returned by GET /api/name
OWNER_FIRST_NAME=First
OWNER_LAST_NAME=Last
# Optional: skip the ipify lookup and return this from GET /api/server-ip
# SERVER_PUBLIC_IP=

# Button 2: course pixel stream relayed to the app on ws(s)://<host>/ws
UPSTREAM_WS_URL=wss://8.229.22.124
# UPSTREAM_WS_INSECURE=true   # only if the upstream uses a self-signed certificate
